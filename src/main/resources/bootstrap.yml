spring:
  application:
    name: configserver

encrypt:
  failOnError: false
  keyStore:
    location: classpath:server.jks
    password: ${KEYSTORE_PASSWORD:letmein}
    alias: mytestkey
    secret: changeme